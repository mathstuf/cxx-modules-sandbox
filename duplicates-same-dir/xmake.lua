target("duplicates-same-dir-1")
    set_kind("$(kind)")
    add_rules("c++")

    add_files("*.mpp")

target("duplicates-same-dir-2")
    set_kind("$(kind)")
    add_rules("c++")

    add_files("*.mpp")